# Optional unless you use the support modal "Send note" feature.
# Sets the mailto recipient for the support note field.
NEXT_PUBLIC_SUPPORT_EMAIL=your-email@example.com

# Optional unless you use the Map tab.
# Required for geocoding, place autocomplete, and commute routing.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-api-key
