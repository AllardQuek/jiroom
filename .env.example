# Optional: support modal "Send note" mailto recipient
# Set this to enable the note field in the support modal.
NEXT_PUBLIC_SUPPORT_EMAIL=your-email@example.com

# Optional: Google Maps API key
# Set this to enable map features (geocoding, commute routing).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-api-key
